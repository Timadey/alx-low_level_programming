#include <string.h>
#include "main.h"
/**
 * _strlen - returns the length of a string.
 * @s: the character whose length will be printed
 * Return: length of the character in int
 */
int _strlen(char *s)
{
	int lnt = strlen(s);

	return (lnt);
}
