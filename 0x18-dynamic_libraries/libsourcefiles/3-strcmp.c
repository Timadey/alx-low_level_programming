#include "main.h"
#include <string.h>
/**
 * _strcmp - compares two strings
 * @s1: first string
 * @s2: second string
 * Return: number of difference
 */
int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
