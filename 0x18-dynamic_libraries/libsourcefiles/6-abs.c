#include <stdlib.h>
#include "main.h"

/**
 * _abs - computes the absolute value of an integer.
 * @num: the number to find it's absolute value
 * Return: absolute value of int type
 */
int _abs(int num)
{
	return (abs(num));
}
