/**
 * mul - multiplies two integers
 * @a: the left hand side integer
 * @b: the right and side integer
 *
 * Return: the result of multiplication in int
 */
int mul(int a, int b)
{
	return (a * b);
}
